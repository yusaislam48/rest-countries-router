import Countries from '../Countries/Countries';
import Loading from '../Loading/Loading';

const Home = () => {
    
    return (
        <div>
            <Countries></Countries>
            <Loading></Loading>
        </div>
    );
};

export default Home;